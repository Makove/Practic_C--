#include <iostream>
#include <math.h>
#include <cmath>
using namespace std;

int main(){
	setlocale(LC_ALL, "rus");
	/*
	//1 task 
	int n, b;
	cout << "Введите n, для которого нужно найти ближайшее целое число делящееся на b, которое тоже введите: ";
	cin >> n >> b;
	n = n + 1;
	while (n%b != 0)
	{
		n++;
	}
	cout << "Ближайшее целое число делящееся на b: " << n;
	
	//2 task 
	int x, y;
	cout << "Введите длины катетов: ";
	cin >> x >> y;
	x = sqrt(x*x + y*y);
	cout << "Длинна гипотенузы: " << x;
	
	//3 task 
	int a;
	cout << "Введите трёхзначное число: ";
	cin >> a;
	cout << "Сумма цифр введённого трёхзначного числа: " << a/100 + a%10 + a/10%10 << ". Произведение цифр введённого трёхзначного числа: " << (a/100) * (a%10 )* (a/10%10);
	
	//4 task 
	int x1, y1, x2, y2, xn, yn;
	cout << "Введите координаты двух точек в формате: x1 x2 y1 y2 ";
	cin >> x1 >> x2 >> y1 >> y2;
	cout << "Уравнение прямой: (x - " << x1 << ") / " << x2 - x1 << "= (y - " << y1 << ") / " << y2 - y1;   
	
	//5 task 
	int m, s, n, y, p;
	cout << "Введите разме кредита, на сколько лет взят и под сколько процентов: " ;
	cin >> n >> y >> p;
	m = (n * p * pow((1+p),y)) / (12 * (pow(1+p,y) - 1));
	s = m * 12 * y;
	cout << "Месячная выплата: " << m << ", суммарная выплата: " << s;
	
	//6 task 
	int a, b;
	cout << "Введите два числа: ";
	cin >> a >> b;
	a = a + b;
	b = a - b;
	a = a - b;
	cout << "Новые значения а и б: " << a << ", " << b;
	
	//7 task 
	int a;
	cout << "Введите четырёхзначное число abcd: ";
	cin >> a;
	cout << "Сумма ab + cd = " << (a/1000*10+a/100%10) + (a/10%10*10+a%10);
	
	//8 task 
	int r, h;
	cout << "Введите радиус бака и его высоту: ";
	cin >> r >> h;
	cout << "Вам понадобится краски на площадь: " << 2*M_PI*r*h + M_PI*r*r;
	
	//9 task 
	float a, b, c, d;
	cout << "a) Введите ширину и длинну прямоугольника ";
	cin >> a >> b;
	cout << "Периметр прямоугольника: " << 2*(a+b) << endl;
	cout << "б) Введите длины сторон треугольника: ";
	cin >> a >> b >> c;
	cout << "Периметр треугольника: " << a+b+c<< endl;
	cout << "в) Введите длины сторон произвольного четырёхугольника: ";
	cin >> a >> b >> c >> d ;
	cout << "Перимет четырёхугольника: " << a+b+c+d;
	
	//10 task 
	float r;
	cout << "Введите радиус: ";
	cin >> r;
	cout << "Объём шара: " << (4*M_PI*pow(r, 3))/3 << ", площадь поверхности: " << 4*M_PI*pow(r, 2);
	
	//11 task 
	float R, h;
	cout << "Введите радиус и высоту цилиндра: ";
	cin >> R >> h;
	cout << "Площадь поверхности цилиндра равна: " << 2*M_PI*R*h + 2*M_PI*R*R << ", объём цилиндра равен: " << M_PI*R*R*h;
	
	//12 task 
	float R, L;
	cout << "Введите радиус основания и высоту: ";
	cin >> R >> L;
	cout << "Площадь осевого сечения: " << R*(sqrt(L*L-R*R));
	
	//13 task 
	float R, L;
	cout << "Введите радиус основания и высоту:: ";
	cin >> R >> L;
	cout << "Площадь осевого сечения: " << R*L;
	
	//14 task 
	float a, h, Sb, So, l, r;
	cout << "Введите сторону основания и высоту: ";
	cin >> a >> h;
	r = a/(2*sqrt(3));
	l = sqrt(a*a+h*h);
	So = a*a*sqrt(3)/4;
	Sb = a*l*3/2;
	cout << "площадь поверхности: " << So+Sb << ", объём пирамиды: " << So*h/3;
	
	//15 task 
	float a, h, Sb, So, l, r;
	cout << "Введите сторону основания и высоту: ";
	cin >> a >> h;
	So = 3*sqrt(3)*a*a/2;
	r = a*sqrt(3)/2;
	l = sqrt(a*a+h*h);
	Sb = a*l*3;
	cout << "площадь поверхности: " << So+Sb << ", объём пирамиды: " << So*h/3;
	*/
	
	return 0;
}
